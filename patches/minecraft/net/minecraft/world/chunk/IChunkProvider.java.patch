--- before/net/minecraft/world/chunk/IChunkProvider.java
+++ after/net/minecraft/world/chunk/IChunkProvider.java
@@ -1,6 +1,11 @@
 package net.minecraft.world.chunk;
 
+import net.minecraft.world.chunk.storage.IChunkLoader;
+import net.minecraft.world.gen.IChunkGenerator;
+
 import javax.annotation.Nullable;
+import java.util.Collection;
+import java.util.function.Consumer;
 
 public interface IChunkProvider
 {
@@ -9,9 +14,39 @@
 
     Chunk provideChunk(int x, int z);
 
+    Chunk provideChunk(int x, int z, Consumer<Chunk> runnable, boolean generate);
+
     boolean tick();
 
     String makeString();
 
     boolean isChunkGeneratedAt(int x, int z);
+
+    // VoidStudio coded start
+    int getLoadedChunkCount();
+
+    IChunkLoader getChunkLoader();
+
+    IChunkGenerator getChunkGenerator();
+
+    void removeChunkFromQueue(int x, int z);
+
+    void addLoadedChunk(Chunk chunk);
+
+    Chunk getChunkIfLoaded(int x, int z);
+
+    Collection<Chunk> getLoadedChunks();
+
+    @Nullable Chunk loadChunk(int x, int z);
+
+    @Nullable Chunk loadChunk(int x, int z, Runnable runnable);
+
+    boolean unloadChunk(Chunk chunk, boolean save);
+
+    boolean isUnloading(int x, int z);
+
+    void queueUnloadAll();
+
+    void queueUnload(Chunk chunk);
+    // VoidStudio coded end
 }
